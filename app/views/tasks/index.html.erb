<h1>Tasks</h1>
<% if @tasks.present? %>
  <ul>
    <% @tasks.each do |task| %>
  <li> <%= link_to task.title, task_path(task) %>
      </li>

      
    <% end %>
  </ul>
<% else %>
  <p>no tasks</p>
<% end %>